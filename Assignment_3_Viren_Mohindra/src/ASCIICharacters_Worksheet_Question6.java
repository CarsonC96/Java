/* Viren Mohindra
 * 6. Generate a random number from 65 through 91, thinking of the number as an ASCII code characters.
 * For 10 different seeds, see how many numbers are needed to generate your name in the correct sequence.
 */

public class ASCIICharacters_Worksheet_Question6 {

	public static void main(String[] args) {
		
		//Random rand = new Random();
		//int random = rand.nextInt(91) + 65; // 91 is the maximum and the 65 is our minimum
		String name = "Viren Mohindra";
		
		for (int i=0; i>100; i++) {
			Character.toString ((char) i);
			if (name.equals(new String(name))) {
				
			};
		}
		
	}
}
